> 工作草稿，已由 `docs/experiments/visual_music_audit_20.md` 的最终问题表取代。

# 建筑几何审查问题表（提案片段）

`final-frozen/source_identity.json` 标记冻结目标为 compiler `3.3.1`；更新的 `verified-frozen/source_identity.json` 为 `3.3.2`，正在做 portable、hole-free decomposition 验证。`after-frozen` 是被拒绝的中间导出，只用于记录失败模式；本片段不把任何 GLB 视觉结果写成通过。

| # | 原始缺陷或触发 | 改动与归属 | 当前残余限制 | 支撑路径 |
|---:|---|---|---|---|
| 1 | **西侧半圆楼板环自交。** Carefree（MAS-SLAB）原始边界从 south 端进入 west 端，弧线却从 north 端反向走回，闭合时产生非邻接边相交。 | 归属 `backend/app/datums.py::_plate_polygon`：west apse 改为 south → west → north 的遍历。`backend/tests/test_plate_ring_validity.py` 用独立的非邻接线段检查、正面积和向西外凸检查覆盖多个 family 与分数层级。 | 二维环测试只证明拓扑与面积；`final-frozen` 3.3.1 的 GLB 仍需视觉复核。 | `backend/app/datums.py`；`backend/tests/test_plate_ring_validity.py`；`artifacts/visual_audit/2026-09-03/baseline-frozen/geometry/building-v3-ba8c48930674/building_model_v3.json`；`artifacts/visual_audit/2026-09-03/final-frozen/source_identity.json` |
| 2 | **Shapely 洞口在 GLB 中被填回。** 中间导出把 outer/hole ring 以相反方向交给现有 `_keyhole`，源多边形仍有效，挤出后洞口却被填实；跨边界洞口和重叠洞口也曾影响组件保留。 | 归属 `backend/app/plan_regions.py`：`_ring` 统一源 ring 为 CCW；`blender/import_building_model_v3.py::_keyhole` 继续负责反转 hole。`extrusions` 保留所有连通组件、对非法 ring 显式报错。 | 3.3.2 的 `verified-frozen` 正在验证 portable、hole-free decomposition；被拒绝的 `after-frozen` 只能作为缺陷证据，最终洞口形态仍待视觉检查。 | `backend/app/plan_regions.py`；`blender/import_building_model_v3.py`；`backend/tests/test_circulation_clearance.py`；`docs/decisions/0018-circulation-clearance-and-visual-regression.md`；`artifacts/visual_audit/2026-09-03/verified-frozen/source_identity.json` |
| 3 | **楼梯洞口、吊顶洞口与梁的协调仍有风险。** 原流程曾让踏步穿过 floor slab/ceiling；现有梁仍可能进入楼梯审查体积。 | `backend/app/compiler_v3.py` 由同一 `_core_layout` 生成 stair、opening 和 reservation；`plan_regions` 做精确 union/difference；`geometry_review` 对旋转实体与 z 区间独立检查。 | 当前模型明确记录“opening-edge framing and structural member rerouting remain unresolved”；`SP-STAIR-HEAD-CLEARANCE` 仍为 `unevaluated`，梁截面深度与 roll 尚未完成实体级结论。 | `backend/app/compiler_v3.py`；`backend/app/geometry_review.py`；`backend/app/spatial_rules.py`；`artifacts/visual_audit/2026-09-03/final-frozen/tracks/couperin-harpsichord/response.json` |
| 4 | **占用楼层的楼梯服务覆盖不能从碰撞计数推断。** 旧 centroid fallback 会把 landing 放到板外；删掉不可行楼梯也会制造“零碰撞”的假象。 | `core_anchors` 选择实际 plate/void 内可达的最长 stair run；`_emit_circulation` 写入 `unreached_levels`，`_assemble` 明示“no emitted stair route”。测试比较实际发出的 flight 与 layout，并验证 tiny plan 不生成 centroid stair。 | 当前 `final-frozen` Couperin 模型 `building-v3-d5b3420190c5` 的 6 个 occupied levels 均有服务，landing 为 `L01–L07` 与 `LND2-L01/L02`。未来缩进或音乐洞口造成无可行核心时，仍须保留未服务楼层与覆盖计数。 | `backend/app/compiler_v3.py`；`backend/tests/test_circulation_clearance.py`；`artifacts/visual_audit/2026-09-03/final-frozen/geometry/building-v3-d5b3420190c5/building_model_v3.json` |
| 5 | **电梯曾与楼梯空间耦合，且缺少可用设备语义。** | `core_anchors` 的 `_place_lift` 独立在楼梯四侧搜索真实 2.6 m 井；`core_reservations` 与 emitted shaft 使用同一 lift anchor；测试检查踏步不侵入 lift wall。 | `final-frozen` 的 limitation 明确写有 lift doors/equipment 未提供；连续井道、门和设备尚未构成可运行电梯。 | `backend/app/compiler_v3.py`；`backend/tests/test_circulation_clearance.py`；`artifacts/visual_audit/2026-09-03/final-frozen/geometry/building-v3-d5b3420190c5/building_model_v3.json` |
| 6 | **v2 Blender 导出会因对象预算阻断，v3 仍应继续出资产。** Drozerix baseline 记录 `BlenderExportError`；后续隔离日志给出真实触发：`Source object budget exceeded: 550 > 500`。 | `backend/app/pipeline.py` 单独捕获 v2 `BlenderExportError`，随后继续执行 `compile_building_model_v3` 与 v3 export；`backend/app/blender_export.py` 与 `backend/app/blender_export_v3.py` 保持两条导出边界。 | Drozerix final response 中 v2 Blender artifacts 为 `blocked`、`blender_web_preview` 为 `fail`，同时 `model_asset_v3` 已有 GLB；该资产仍是 `presentation_only`，`visual_review` 仍待完成。3.3.2 的 Valse verified-frozen 已生成 v3 资产，接受阶段仍 blocked。 | `backend/app/pipeline.py`；`backend/app/blender_export.py`；`backend/app/blender_export_v3.py`；`artifacts/visual_audit/2026-09-03/baseline-frozen/tracks/drozerix-rndd/result.json`；`artifacts/visual_audit/2026-09-03/final-frozen/tracks/drozerix-rndd/response.json`；`artifacts/visual_audit/2026-09-03/verified-frozen/tracks/valse-gymnopedie/response.json` |
| 7 | **音乐洞口与紧凑上层会使 brief 无法完整落位。** | `_carve_and_allocate` 先应用真实 carve 再选核心和分配；拒绝 carve 时恢复原 music voids；`program.py` 保留 `UnplacedSpace`。 | 被拒绝的 `after-frozen` museum 样本仅交付 1,635/2,480 m²（66%），Permanent gallery 与 Temporary gallery 未落位；最终 3.3.1 仍需按每条录音分别汇报。 | `backend/app/compiler_v3.py`；`backend/app/program.py`；`backend/app/archetypes.py`；`artifacts/visual_audit/2026-09-03/after-frozen/geometry/building-v3-e8dba227e877/building_model_v3.json` |
| 8 | **楼梯几何检查通过不代表疏散条件成立。** Couperin final response 的 life-safety rollup 仍列出 L01 出口数量、L01 疏散容量和 L04 出口数量三个 blocker。 | 该结论归属 `backend/app/life_safety.py` 的独立图检查；geometry review 保留碰撞结果，不能升级 life-safety 状态。 | 出口数量与容量仍需改造方案或专业审查；当前记录不得写成可许可或安全结论。 | `backend/app/life_safety.py`；`artifacts/visual_audit/2026-09-03/final-frozen/tracks/couperin-harpsichord/response.json`；`docs/decisions/0018-circulation-clearance-and-visual-regression.md` |

可直接引用的独立回归验证：`artifacts/visual_audit/2026-09-03/targeted_test_results.txt` 记录了当前代码下 7 个 massing family 的 landing flush/inside-plate 定向测试 `1 passed`（353.90 s）；这项数值测试不替代 final-frozen 的视觉复核。
